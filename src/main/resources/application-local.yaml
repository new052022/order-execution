spring:
  datasource:
    username:
    password:
    url: jdbc:postgresql://localhost:5432/order_execution

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka

secret-key:

algorithm: